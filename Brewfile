# Make sure using latest Homebrew
update

# Update already-installed formula
upgrade

# Add Repository
tap phinze/homebrew-cask # for brew-cask
tap homebrew/binary # for packer

# Packages
install zsh || true
install git || true
install gist || true
install tig || true
install rmtrash || true
install coreutils
install wget || true
install tmux || true
install proctools || true
install markdown || true
install tree || true
install nkf || true
install brew-cask || true
install packer || true
install autoconf || true
install ghc || true
install libyaml || true
install libxml2 || true
install libxslt || true
install boot2docker || true
install docker || true
install hub || true

# .dmg
cask install google-chrome
cask install virtualbox
cask install vagrant
cask install chefdk
cask install crashlytics

# Remove outdated versions
cleanup